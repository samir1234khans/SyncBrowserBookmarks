{
  "name": "sync-browser-bookmarks",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "extension",
    "backend"
  ],
  "scripts": {
    "dev:backend": "npm --workspace backend run dev",
    "dev:extension": "npm --workspace extension run dev",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  }
}
